[project]
name = "comfyui-nifty-nodes"
version = "2.1.0"
description = "ComfyUI node pack with a custom Bundle system, smart media loaders, multi-LoRA loader, logic/switch nodes, image tools and more."
license = { file = "LICENSE" }
requires-python = ">=3.9"
dependencies = ["color-matcher", "imageio-ffmpeg"]

[project.urls]
Repository = "https://github.com/Stibo/comfyui-nifty-nodes"

[tool.comfy]
PublisherId = "stibo"
DisplayName = "Nifty Nodes"
Icon = ""
